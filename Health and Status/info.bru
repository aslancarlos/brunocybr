meta {
  name: Info
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/info
  body: none
  auth: none
}

tests {
  test("status is 2xx", function() {
    expect(res.getStatus()).to.be.within(200, 299);
  });
}

docs {
  Informações básicas sobre o servidor.
}
